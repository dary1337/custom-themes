/**
 * No setup logic — `defineModel` is a compile-time macro that must live in the
 * `<script setup>` block. This file is kept for symmetry with other components
 * and to host any future props/types.
 */
export type ThemeSwitchModel = boolean;
